<%- include('partials/header') %>
    <main>
        <h1><%= title %></h1>
    <h2>Upcoming Service Projects</h2>
    <ul class="content-grid">
        <% project.forEach(project => { %>
            
        <li class="grid-card"><%= project.project_date %><strong><%= project.title %></strong>
            <%= project.description %>
        </strong>: 
    <%= project.location %></li>
    <% }); %>
        
    </ul>
    </main>
    <%- include('partials/footer') %>